// You need to define a Car type. This type should include the properties model (string), year (number), and isElectric (boolean). How would you define this type?

interface Car {
    model: string;
    year: number;
    isElectric: boolean;
}