// What is the purpose of the interface defined in the following TypeScript code snippet and what properties does it contain?

// The purpose of this interface is to define a person's basic information through an interface. It contains three properties: firstName (of type string), lastName (of type string), and age (of type number).

interface Person {
  firstName: string;
  lastName: string;
  age: number;
}